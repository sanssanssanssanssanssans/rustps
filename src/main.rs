#![allow(dead_code)]
#![allow(unused)]
mod fastio;
mod data_structures;
mod sort;
mod math;
use fastio::{FastI, FastO};

fn main() {
    let mut input = FastI::new();
    let mut output = FastO::new();

    
}